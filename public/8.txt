----------------------------443181467065257681909892
Content-Disposition: form-data; name="ppp"; filename="main.c"
Content-Type: text/xml

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

#define BUFFER_SIZE 1 

char    *get_next_line(int fd)
{
	char    line[65000000] = {0};
	int     i = 0;
	while (read(fd, &line[i], 1) > 0) {if (line[i++] == '\n') {break ;}} 
	return ((line[0] != 0 && BUFFER_SIZE > 0) ? strdup(line) : NULL);
}

int main(){

	char *line = get_next_line(0);
	printf("%s\n", line);
}

----------------------------443181467065257681909892
Content-Disposition: form-data; name="5"
Content-Type: fghfg

4564
----------------------------443181467065257681909892
Content-Disposition: form-data; name="kk"; filename="main.c"
Content-Type: text/x-c

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

#define BUFFER_SIZE 1 

char    *get_next_line(int fd)
{
	char    line[65000000] = {0};
	int     i = 0;
	while (read(fd, &line[i], 1) > 0) {if (line[i++] == '\n') {break ;}} 
	return ((line[0] != 0 && BUFFER_SIZE > 0) ? strdup(line) : NULL);
}

int main(){

	char *line = get_next_line(0);
	printf("%s\n", line);
}

----------------------------443181467065257681909892
Content-Disposition: form-data; name="7"
Content-Type: fgh

o
----------------------------443181467065257681909892--
